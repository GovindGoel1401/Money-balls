version: '3.8'

services:
  web:
    build: .
    ports:
      - "8001:8001"
    environment:
      - NODE_ENV=development
    networks:
      - quasar-network

networks:
  quasar-network:
    driver: bridge
